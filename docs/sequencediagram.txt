# the atlantica server-client connection
# sequence diagram source code
# render using https://sequencediagram.org/

title Atlantica

participant Client
participant Server



Client->>Server:TCP Connection request
Client<<--Server:TCP Connection accept
Client->>Server:encryption request [pub_key]
box left of Server:Generate random\nsession key
Client<<--Server:encrypted session key

==end encryption handshake==
abox over Client,Server:ChaCha20-Poly1305\nencrypted from here on

Client<<-Server:Authentification request
Client-->>Server:client name

box left of Server:Check if name is\nalready connected
alt name is already connected
Client<<-Server:fail: this client already connected
box over Client,Server:Exit
else name is not signed with right public key
Client<<-Server:fail: authentification failed
end

==End authentifiction==


abox over Client,Server:Game communictation